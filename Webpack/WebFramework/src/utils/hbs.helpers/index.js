var compare  = require('./compare')
var formatTime = require('./formatTime')
var isDevelopment = require('./isDevlopment')
var isEq = require('./isEq')
var section = require('./section')
var strLen = require('./strLen')

module.exports = {
    compare,
    formatTime,
    isDevelopment,
    isEq,
    section,
    strLen
}